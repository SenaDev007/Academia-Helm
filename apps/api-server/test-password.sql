-- Test de connexion dans pgAdmin 4
-- Exécutez cette requête dans l'éditeur SQL de pgAdmin 4

-- Vérifier l'utilisateur actuel
SELECT current_user;

-- Vérifier la base de données actuelle
SELECT current_database();

-- Si vous pouvez exécuter ces requêtes, la connexion fonctionne
-- Notez le mot de passe que vous utilisez pour vous connecter dans pgAdmin 4
