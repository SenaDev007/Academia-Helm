/**
 * Offline Indicator
 *
 * ⚠️ DÉSACTIVÉ TEMPORAIREMENT — La bannière orange apparaissait même
 * quand la connexion était juste instable, bloquant l'application.
 *
 * Ce composant ne retourne plus rien (null).
 */

'use client';

export default function OfflineIndicator() {
  // ⚠️ Mode offline désactivé — ne rien afficher
  return null;
}
