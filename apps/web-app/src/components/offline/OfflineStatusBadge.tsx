/**
 * Offline Status Badge
 *
 * ⚠️ DÉSACTIVÉ TEMPORAIREMENT — Le mode offline est désactivé.
 * Ce composant ne retourne plus rien (null).
 */

'use client';

export function OfflineStatusBadge() {
  // ⚠️ Mode offline désactivé — ne rien afficher
  return null;
}
